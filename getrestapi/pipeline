pipeline{

    agent any

   // environment {
       // DOCKERHUB_CREDENTIALS=credentials('dockerhub')
    //}

       stage('Build') {
    steps {
        sh 'pwd'
        sh 'ls'
        sh 'cd "getrestapi" && ls'
        sh 'docker build -t flask  -f Dockerfile .'
    }
       }
}
}
